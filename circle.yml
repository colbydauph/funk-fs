# Spec for CircleCI test running
# https://circleci.com/docs/1.0/configuration
dependencies:
  pre:
    - npm i -g npm

machine:
  node:
    version:
      8.9.1

# Customize test commands
test:
  override:
    - npm run verify